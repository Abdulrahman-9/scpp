# خطة موجة العميل الأولى — جلسة MDOC (شركة نفط الوسط)

> **الحالة:** مقترحة — بانتظار إقرار العميل على «قرارات التأكيد» أدناه ثم التنفيذ
> **التاريخ:** 2026-08-20
> **المصدر:** جلسة العميل — 21 طلباً + طلب تحديث بصري عام
> **خط الأساس:** شجرة ما بعد الختام (381 اختباراً · typecheck 6 مشاريع · build ✅) — **تُثبَّت commit قبل أول تعديل (المرحلة صفر)**

---

## ١. التفسير الحاكم — النموذج المالي الثلاثي (يفكّ الطلبات 3 + 4 + 21 + 18 معاً)

العميل يدير عمله بسلّم موافقات لا بدورة كلف مجرّدة:

| الطبقة | النطاق (مثال العميل) | الجهة | سلوك النظام |
|---|---|---|---|
| ط١ — ضمن صلاحية المشغّل | ≤ 5M$ | المشغّل | متابعة وتدقيق فقط، **بلا موافقة** |
| ط٢ — فوق الصلاحية حتى سقف JMC | 5–10M$ | **اللجنة المشتركة JMC** | بوابة موافقة JMC |
| ط٣ — فوق سقف JMC | > 10M$ | **MDOC (نفط الوسط)** | بوابة موافقة الشركة الأم |

**المبدأ الهندسي:** محرّك SCPP لا يُهدم — «فوق صلاحية المشغّل» هو نفسه مشغّل دورة MCT الحالي؛ نعيد **تقديمه** كسلسلة موافقات مسماة بلغة العميل، ونضيف السقف الثاني (JMC/MDOC). شاشة MCT الحالية تُستبدل بشاشة «سلسلة الموافقات» (إخفاء تام للواجهة بقرار ق3)؛ محرّك MCT وحقوله في المخزن يبقيان بنيةً تحتية، واستبدال كون البيانات نفسه سُجّل قيداً في سجل التدقيق (SEED_MIGRATION_V9) وفاءً بقانون الإلحاق-فقط 8.1-e.

**إعادة التسمية (طلب 18):** كل ظهور لـROC (تسميات، أدوار معروضة، شاشات، تقارير) يصير **MDOC** — طبقة العرض والترجمة أولاً، مع قرار مؤكَّد من العميل حول أعماق النموذج (أسماء الأدوار في الخادم/قاعدة البيانات).

---

## ٢. قرارات تأكيد مطلوبة من العميل قبل التنفيذ (أسئلة الجلسة القادمة)

| # | القرار | **حكم العميل (2026-08-20)** |
|---|---|---|
| ق1 | سقوف الطبقات | ✅ **عامة للنظام** — سلّم واحد لكل المشغّلين (بذرة: ≤5M مشغّل · >5M–≤10M لجنة JMC · >10M نفط الوسط MDOC؛ قابلة للتعديل بفعل محوكم لاحقاً) |
| ق2 | عمق ROC→MDOC | ✅ **كامل حتى أسماء الأدوار** — `ROC_ADMIN`→`MDOC_ADMIN` في Prisma والخادم والجلسة وسجل القدرات والعرض؛ صفوف التدقيق التاريخية تُقرأ بتسامح |
| ق3 | بيانات MCT | ✅ **إخفاء تام + بذرة تجريبية جديدة تخص نفط الوسط** — يُتبنّى سجل الحقول الحقيقي الـ13 من المخطط التسليمي (الأحدب/بدرة/منصورية/شرق بغداد… 12 شركة مشغّلة) بدل الحقول الجنوبية؛ محرّك SCPP يبقى، الشاشة تُستبدل بسلسلة الموافقات |
| ق4 | «المطابقة» | ✅ **بيان المعنى** — تسمية أوضح + سطر تعريف ظاهر + شرح تحتي، مع فلتر الحقل |
| ق5 | قائمة الوزارة | ⏳ معلّق — يُنفَّذ الفصل التوضيحي المقترح حتى يرد تعريف العميل |
| ق6 | تخصصات الموردين | ⏳ معلّق — تُستنبط من نطاقات العقود مؤقتاً حتى يزوّدنا العميل بالقائمة الرسمية |
| ق7 | حذف الجهات | ✅ **أرشفة** |

---

## ٣. المراحل

### المرحلة ٠ — التثبيت وخط الأساس «حجم: يوم»
- [x] commit التسليم الحالي على `main` (`f90a0db`، 150 ملفاً) + وسم `v1.0-delivery`. ✅ 2026-08-20
- [x] فرع `client-wave-1` للموجة كلها. ✅
- [x] تحديث الذاكرة وربط هذه الخطة بها. ✅

### المرحلة ١ — النموذج المالي الثلاثي + هوية MDOC (الطلبات 3، 4، 18، 21) «الأثقل — الجذر»
- [x] ~~سقوف لكل مشغّل~~ ← **حكم ق1: سلّم عام** — `State.approvalTiers` مبذور {5M/10M} + ترحيل مفتاح v8→v9 يحفظ التدقيق ويسجّل استبدال الكون قيداً. ✅
- [x] `approvalTierFor(valueUSD, tiers)` في `scpp-rules` + غلاف `tenderApprovalTier` — سقوف شاملة عند الحد، فشل مغلق إلى MDOC (سلّم مقلوب/قيم فاسدة/غياب) — مستقلة عمداً عن FA الحقلية (§7.1 طبقة أخرى). ✅
- [x] نموذج «إدخال مشغّل جديد» يعرض السلّم العام للقراءة (ق1: لا سقوف لكل مشغّل). ✅
- [x] شاشة **«سلسلة الموافقات»** `#/admin/approvals` (هيكل السجل + KPI + فلاتر + `TierPill` + `SectionExplainer` الأول) وإعادة توجيه `#/admin/mct`. **حكم ق3 النهائي: إخفاء تام لا أرشيف مقروء** — بيانات MCT القديمة استُبدلت مع بقية الكون بهجرة مسجَّلة قيداً؛ محرّك MCT باقٍ بنيةً. ✅
- [x] ROC→MDOC حتى الجذور (Prisma enum + هجرة، الحارس، الجلسة v2→v3، سجل القدرات، الشاشات، التقارير؛ تسامح عرض للسجل التاريخي؛ أسماء المحرّك SCPP الاصطلاحية باقية عمداً). ✅
- [x] لغة الطبقات في بوابة التصديق وقائمة القرار + بيان «المطابقة» وفلتر الحقل (ق4). ✅
- [x] اختبارات: حدود 5M/10M الدقيقة، الفشل المغلق، ترحيل v8→v9، ثوابت البذرة (12 شركة/13 حقلاً/سوبر واحد). **455 اختباراً أخضر.** ✅
- [ ] **دين مسمّى واحد بنصفين — سلّم الموافقات (تحرير السقوف + مصدر السلّم في الخادم):** لا فعل محوكم ولا نقطة نهاية تحرّك السقفين اليوم، **ولا نموذج سقوف في الخادم أصلاً**: بوابة `TendersService.assertTierAuthority` تقرأ ثابت المحرّك `DEFAULT_APPROVAL_TIERS` مباشرةً، وهو مصدرها المعتمد الوحيد، بينما الواجهة تقرأ `state.approvalTiers` (حالة قابلة للتحرير مبذورة من الثابت نفسه). النصفان **دين واحد** لأنهما يُحَلّان معاً أو يتباعدان معاً: يوم يُتاح تحريك السقفين تصير حالة الواجهة متحرّكة فعلاً بينما يبقى الخادم على ثابته، فيقول الزرّ المعطَّل شيئاً ويقول الـ403 غيره. اليوم هما الكائن نفسه، وهذا **ثابت مثبَّت لا مصادفة**: `apps/api/test/tenders.service.spec.ts` (يفحص الحدود عبر البوابة الحقيقية) و`apps/web/test/jmcRole.test.ts` (هوية `SEED_APPROVAL_TIERS`)، وتعليق متقاطع على الموقعين (`tenders.service.ts` ↔ `store.tsx`) كي لا يتحرّك أحدهما وحده. يُرتَّب مع ديون api-mode.

### المرحلة ٢ — دمج الحقل والمشغّل + الجهات (الطلبات 9، 14) «حجم: متوسط»
- [x] معالج واحد **«إضافة مشغّل وحقوله»** (`admin/OperatorFieldsWizard.tsx`): ثلاثة أقسام مرقّمة مبوَّبة في حوار واحد (بيانات المشغّل ← حقوله المتكرّرة ← المراجعة والمسوّغ) مع شريط سلّم الموافقات العام للقراءة (استمرار الطلب 4). يحلّ محلّ حوار «أضف مشغّلاً» في شاشة المشغّلين. **التثبيت بالأفعال القائمة** — `CREATE_OPERATOR` ثم `CREATE_FIELD` لكل صف (كلٌّ يُنشئ الحقل مع عقد خدمته §7.1) — متتابعة ومُنتظَرة، بلا تراجع جماعي (سجل إلحاقي 8.1-e) وبالتحقّق من المخزن نفسه لا من نتيجة الإرسال. ✅
- [x] مسار التعديل اللاحق: فعل محوكم جديد `RENAME_FIELD` (الاسمان المعروضان فقط — الرمز مذكور في رموز المناقصات والعقود، والشركة المالكة يحدّدها عقد الخدمة). تعديل FA قائم أصلاً عبر `SET_CONTRACT_FA`. ✅
- [x] **نموذج الأرشفة (ق7)**: `archived?` على الحقل والجهة + `ARCHIVE_FIELD`/`RESTORE_FIELD`/`ARCHIVE_VENDOR`/`RESTORE_VENDOR` — الحقل يُرفض وعليه مناقصة **جارية** (البوابة تسمّي العدد)، والجهة تُؤرشف مهما كان سجلّها (التاريخ ثابت). أثر موثّق (`FieldEvent`/`VendorEvent`) وتدقيق ذاتي مطبَّق/مرفوض. ترحيل المفتاح v10→v11 (أعلام اختيارية، مرور كامل بلا قيد مُختلَق). ✅
- [x] شاشة الجهات: العرض الافتراضي **كل** الجهات (المؤرشفة ظاهرة مكتومة + شريحة «المؤرشفة») + «إضافة جهة» (`CREATE_VENDOR` — بلا خيار شركة حكومية: الخمس بذرة نظامية) + «تعديل» لكل صف يفتح ملف الجهة + الأرشفة/الاستعادة في صف الحوكمة داخل الملف مع لافتة «جهة مؤرشفة — سجلّها محفوظ». ✅
- [x] بوابات صادقة: الجهة المؤرشفة تخرج من مجموعة `selectableVendors` بحاجز `registry` (لا 10.4 ولا 14.3 — الأرشفة ليست عقوبة)، والحقل المؤرشف يخرج من مُنتقي `RequestWizard` وفلتر بوّابة المشغّل؛ سجلّات الإدارة تبقى كاملة عمداً كي يظلّ السجل التاريخي قابلاً للفلترة. ✅
- [x] اختبارات الدمج والأرشفة: **545 اختباراً أخضر** (خط الأساس 477) عبر `archive.test.ts` و`archiveScreens.test.tsx` و`mergedWizard.test.tsx` وتوسعة `migration.test.ts`/`approvals.test.ts`. ✅
- [x] **تصحيحات مراجعة المرحلة ٢ (2026-08-20): 574 اختباراً أخضر.** آلة الصدق في معالج الدمج أُعيد تصميمها: `fieldIdsFor` صار غير قابل للتصادم (رمزان يذوبان في نفس الـslug كانا يتقاسمان معرّفاً واحداً)، و`planChain` يحكم على السلسلة كاملةً قبل أول إرسال (تكرار الرموز والمعرّفات بين الصفوف)، والتحقّق صار **بالرمز من المخزن** بعد كل صف مع التوقّف عند أول فشل — الإيصال يعرض القيود المقروءة من السجل لا قيم النموذج. وملحقاتها: `CREATE_TENDER` يرفض حقلاً مؤرشفاً (قانون لا واجهة)، ملف الجهة نال لافتة «محلي» في وضع الـAPI وبوابة SUPER_ADMIN للأرشفة/الاستعادة، تصحيح ترتيب/أولوية `--wide` (تُثبت 720px في الحزمة المبنية)، اسم تصدير الحقول يحمل نطاق الشريحة، و`.arch-row` استبدل الشفافية بلون توكن (النِسَب ≥4.5:1 مع التحويم).
- [ ] **دين مسمّى:** لا نقاط نهاية لأيٍّ من الأفعال الستة الجديدة — `/api/fields` و`/api/service-contracts` غير موجودتين أصلاً، و`/api/vendors` تعرف الإيقاف والحظر والتقييمات فقط (لا إنشاء ولا علم أرشفة). الستة داخل `CLIENT_ONLY` وكل شاشة تحملها تعلن «محلي» في وضع الـAPI — تُنقل مع بقية ديون api-mode.

### المرحلة ٣ — غرفة المتابعة والداشبوردات (الطلبات 1، 2، 5، 6، 12، 13) «حجم: كبير — واجهة»
- [ ] صناديق غرفة المتابعة الجديدة: عدد المناقصات لكل شركة مشغّلة (نشطة/منجزة/متأخرة/بانتظار موافقة حسب الطبقة) — **كلها مشتقة من المخزن**.
- [ ] بطاقات تفصيل الشركات: أنواع المشاريع وعددها لكل مشغّل (تصنيف حسب `scope` القائم: حفر/هندسة-إنشاء/أخرى + الحقول).
- [ ] إزالة صندوق «مراحل متجاوزة للمخطط» واستبداله بصناديق أدق: موافقات معلّقة حسب الطبقة، عقود حسب نسب الإنجاز، التزام زمني.
- [ ] إحصاءات العقود المحالة في الواجهة الرئيسية (الداشبورد العام): عدد/قيمة العقود المحالة، حسب الطبقة (مشغّل/JMC/MDOC)، والمنجزة (طلب 13).
- [ ] **وضوح تحديث نسب الإنجاز (طلب 12):** لوحة العقد تشرح مصدر النسبة (مراحل العقد المغلقة) + زر «تقدّم مرحلة» بارز بمعاينة أثر النسبة قبل التثبيت — «كيف أتحكم بها» يصير مرئياً.
- [ ] رسوم SVG مشتقة خفيفة (بلا مكتبات ثقيلة): أشرطة مقارنة الشركات، دونات الطبقات، شريط الالتزام — بمواصفة dataviz (تباين ≥3:1 للعلامات، ألوان الدلالة الثابتة).

### المرحلة ٤ — الفلاتر والتقارير والامتثال (الطلبات 7، 8، 10، 15، 16، 17) «حجم: متوسط»
- [ ] فلترة دقيقة شاملة لكل جداول العرض والطباعة: الحقل، المشغّل، الطبقة، الحالة، النطاق الزمني، القيمة (من/إلى)، النوع — على طبقة `registry/` القائمة + **ختم الفلاتر النشطة على كل مطبوع** (يسدّد بند خطة المنهجية م٥).
- [ ] قسم المناقصات: توضيح «المطابقة» (حسب ق4) بتسمية مفهومة + وصف تحتي + فلتر حسب الحقل.
- [ ] شاشة الامتثال الزمني: مواعيد المناقصات والعقود مقابل خططها بما توفّر من بيانات (المشتقات القائمة `scheduleVariancePct`/`stageDevWd`).
- [ ] قائمة الوزارة: فصل بصري وتعريفي (ق5) بين الشركات الحكومية الخمس وقائمة مجهّزي الوزارة، مع شرح مصدر كلٍّ.
- [ ] تقرير الموردين: تصنيف حسب التخصص (ق6) + شريحة قدرة مالية (من بيانات المشاركات/العقود المتاحة — لا اختلاق).
- [ ] إزالة «ضمانات تنتهي خلال 60 يوماً» من التقارير (تبقى كتنبيه مشتق في الجرس فقط إن أراد).

### المرحلة ٥ — الوصول والأدوار المبسّطة (الطلبات 19أ، 19ب، 20، 21) «حجم: متوسط-كبير»
- [x] **دور `JMC_APPROVER` كاملاً حتى الجذر** (سابقة إعادة تسمية MDOC): `enum Role` في Prisma + هجرة دلتا (`ADD VALUE ... AFTER 'MDOC_ADMIN'`، غير مدمِّرة، لا صفّ يُمَس)، ثم `auth.types.ts`/`users/dto.ts`/`LoginDto`، ثم `session.ts` في الواجهة. **النموذج الصادق:** نقطة النهاية التي تصيبها موافقة ط٢ هي `POST /api/tenders/:id/ratify` (و`/return`) — فصار `@Roles` عليهما `MDOC_ADMIN + SUPER_ADMIN + JMC_APPROVER`، **والخدمة** تحكم بالطبقة: `assertTierAuthority` تقرأ سلّم ق1 وتقارنه برتبة الدور (`mayRatifyTier` في `scpp-rules` — تعريف واحد يقرؤه الخادم والمخزن والشاشة). ط٢ ← اللجنة فما فوق · ط٣ ← نفط الوسط فقط · اللجنة على ط٣ تُرفض 403 وتُسجَّل `RATIFY_REFUSED … (tier MDOC)`. ✅
- [x] مرآة الحكم في المخزن: `RATIFY`/`RETURN_WITH_NOTES` صارا ذاتيَّي التدقيق (خرجا من غلاف التدقيق العام) — الرفض بسبب الطبقة يُسجَّل برمز `tier-MDOC` وبالفاعل الثابت، **والحكم يسبق الجدارة** (لا يعرف من لا يملك الطبقة حالة الملف). وبوابة `wz-gate` في `TenderReview` تسمّي الجهة صاحبة القرار ودور الجلسة. ✅
- [x] سجل القدرات: **العدد ثابت 43** عمداً (دور جديد ليس نقطة نهاية) — تغيّرت قائمتا `@Roles` لصفَّي المصادقة والإعادة فقط، فاللجنة تبلغ صلاحيتين محروستين + ثلاث قراءات مفتوحة، لا أكثر. `CAP_REV=gt-2026-08-20b` وتثبيت الاختبار محدَّث بوعي (7 أدوار · نغمة سادسة · `orphanRoleCount` صار 3). ✅
- [x] **البذور (19أ + 19ب):** «حساب المدقق الخارجي» — عنوان وظيفة لا اسم شخص — **عُطِّل ولم يُحذف** مع قيد مسوَّغ على أثره (8.1-e)، ودور `AUDITOR` تُرك بلا حامل مفعّل عمداً كي يكون مؤشّر «أدوار بلا حامل» حيّاً على أول تحميل (نفس منطق «سوبر واحد مفعّل»). وأُضيف عضوان مسمّيان للجنة المشتركة (`jmc.iq`، بلا `operatorId` — `scopeConsistent` قائم) في بذرة الواجهة وبذرة قاعدة البيانات معاً. ✅
- [x] **دمج الثلاث شاشات في قسم «الوصول والأدوار»** (`admin/AccessSection.tsx`): تبويب داخل الشاشة يعيش في العنوان (`?tab=`) بمفردات مغلقة عبر `useHashParams` — «الحسابات» (السجل بسلوكه كما هو) · «الأدوار» (بطاقات جديدة) · «المصفوفة المرجعية» (الـ43 صفاً كاملة، أُبقيت للمدقّق). `#/admin/users` هو العنوان المعتمد و`#/admin/roles` يُعاد توجيهه إلى التبويب. أزرار كبيرة ملوّنة، ودخول واحد في السايدبار بدل اثنين. ✅
- [x] **الحقائق المالية الثلاث لكل دور (21)**: بطاقة لكل دور بلون ورمز واسم وسطر بلغة مفهومة، ثم «يوافق حتى؟ / يشهد القرار؟ / نطاقه؟» بخط كبير — **مشتقّة** من `roleFinancialFacts` التي تقرأ السلّم الحيّ وجدول الرتب نفسه الذي تفرضه البوابة، فلا تعِد بطاقةٌ بصلاحية يرفضها الخادم. الحاملون بعدّادهم وصورهم، و«عرض الصلاحيات التفصيلية» يفتح صفوف السجل الحقيقية. ✅
- [x] **بطاقة «من يوافق على ماذا»** فوق تبويب الأدوار: الجهات الثلاث بحدودها الحيّة ومثال عملي لكل جهة من **مناقصة مبذورة حقيقية** (`tierExample` — محروسة: طبقة بلا طلب تعرض حدودها وتقول ذلك، ولا يُختلق مثال). المبالغ والرموز جُزُر LTR أحادية المسافة. ✅
- [x] اختبارات: **856 اختباراً أخضر** (خط الأساس 785، +71) — بوابة الطبقة في المحرّك والخادم والمخزن، رفض اللجنة على ط٣ مسجَّلاً، تثبيت سجل القدرات، ثوابت البذرة، اشتقاق الحقائق المالية من نفس السلّم، وشاشات القسم الثلاث + إعادة التوجيه. ✅
- [x] **أزرار كبيرة «منح/سحب/تعطيل» — مستوفاة على مستوى الحساب لا على مستوى الدور.** المعيار كُتب أصلاً بلغة الأدوار، والقراءة الصحيحة له أنّ المطلوب أفعال بحجم واضح على **الحساب**: وهي قائمة فعلاً في ملف الحساب `#/admin/users/:id` — «تغيير الدور» و«نقل النطاق» و«تفعيل/تعطيل» و«العامل الثاني»، كلٌّ بحوار مسوَّغ وقيد تدقيق. وهناك تعيش عمداً لا في بطاقة الدور، لأنّ البوابات الثلاث كلّها تحتاج **شخصاً بعينه** لتحكم: تحذير «هذا التغيير يترك دوراً حوكمياً بلا حامل» (`rolesOrphanedBy`)، وقاعدة «سوبر أدمن مفعّل واحد على الأقل»، واتساق النطاق (`scopeConsistent`). زرّ «امنح هذا الدور» على بطاقةٍ سيكون سطح تعديل ثانياً بلا أيٍّ من هذه الحواجز. الوصلة بينهما: كل بطاقة دور تحمل تذييل **«حاملو الدور»** يهبط على `#/admin/users?role=<r>` — سجل الحسابات نفسه مصفّى (`role` مُضاف إلى مفردات العنوان المغلقة في `useHashParams`، وشرائح الأدوار صارت تكتب العنوان أيضاً فلا يختلف ما يُرى عمّا يُنسخ ويُرسَل). **لا سطح تعديل جديد.** ✅
- [ ] **دين مسمّى:** `JMC_APPROVER` خارج `ADMIN_ROLES` في `api/endpoints.ts` عمداً (لا تمنحه أي `@Roles` قراءة العقود/الجهات/التدقيق، وطلبها يُسجَّل رفضاً) — يُضاف يوم يمنحه مُزيِّن حقيقي.

### المرحلة ٦ — الملاحة والتحديث البصري العام (الطلب 11 + طلب «الأحدث والأجمل») «حجم: كبير — تصميم»
- [ ] السايدبار: أقسام رئيسية وثانوية (رئيسية: المتابعة/المناقصات/العقود/الجهات — ثانوية قابلة للطي: التقارير/الامتثال/الوصول/الإعدادات) مع عدّادات حية.
- [ ] **موجة التباين والتفاعل** (بمهارات التصميم الداخلية — سجلّ Product: ألفة مكتسبة لا غرابة):
  - رفع التباين: أحبار أغمق للنص الثانوي، حدود أوضح، ظلال هرمية خفيفة — عبر التوكنات (تبقى المصدر الوحيد).
  - حركة هادفة 150–250ms: انتقالات الفتح/الإغلاق، عدّ تصاعدي للمؤشرات، انزلاق الدُرج، hover بارز للصفوف — مع `prefers-reduced-motion`.
  - تفاعلية اللوحات: صناديق إحصاء قابلة للنقر تهبط على السجل مصفّى، رسوم تُظهر التفاصيل عند التحويم، تبديل مدى زمني.
  - بلا مكتبات ثقيلة جديدة: CSS حديث + SVG مشتق؛ وإن لزمت مكتبة حركة واحدة خفيفة تُسمّى وتُقر أولاً.
- [ ] جولة `polish` ختامية بمعايير المهارة (تباين ≥4.5:1، بؤرة مرئية، فراغات إيقاعية) + لقطات AR/EN لكل شاشة متغيرة.

---

## ٤. قواعد التنفيذ (موروثة ومكمّلة)
1. كل مرحلة: `typecheck` (6 مشاريع) + كامل الاختبارات + build + تحقق متصفح AR/EN + commit.
2. لا اختلاق: كل رقم من المخزن/الخادم؛ كل زر بفعل حقيقي؛ الأرشفة بدل المحو حيثما وُجد سجل.
3. i18n بالتكافؤ التام + أرقام لاتينية (قانون م٠).
4. تغيير شكل المخزن = ترقية مفتاح بترحيل يحفظ التدقيق + اختبار هجرة.
5. الوكلاء المنفّذون Opus بإشراف مركزي؛ ملفات الترجمة تُدمج مركزياً عند التوازي.
