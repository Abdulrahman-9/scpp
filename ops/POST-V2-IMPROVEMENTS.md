# سجل التحسينات التقديرية والنظافة — ما بعد v2

> **الحالة:** مفوَّضة («أفوضك بعد الاكتمال بعمل ما تراه من تحسينات ونظّف الكود») — تُنفَّذ بعد ختم موجة v2
> **المصدر:** كل ما رصدته أعين الوكلاء والمحققين عبر الموجات وبقي خارج نطاقات دفعاته — مجموعاً من تقاريرهم حرفياً

## أ. النظافة (تنفَّذ أولاً — بلا قرارات)

- [ ] **المتغيرات غير المستعملة** المرصودة بفحص `--noUnusedLocals` الاختياري (خارج tsconfig الأساس): `admin/entities.ts`، `Holidays.tsx`، `Operators.tsx`، `Reports.tsx`، `UserProfile.tsx`، `TenderDetail.tsx` — كنسها + تقييم إضافة الفحص إلى typecheck الدائم.
- [ ] **CSS ميت مُعلَّم**: `.op-notif__mark` (بعد حذف زر «مقروء»)، كتلة `.op-toast/.op-toast-wrap/.op-toast__mark` القديمة (استُبدلت بـtv2) — وما سيتجاوزه v2 (يكنسه محققه أصلاً؛ هذه البقايا أقدم منه).
- [ ] **جولة مفاتيح ترجمة يتيمة شاملة** (آخر مسح كامل كان أثناء الموجات — جولة ختامية على 1600+ مفتاح).
- [ ] **`design-v2-ref/`**: يبقى مرجعاً في المستودع أم يُنقل تحت `ops/`؟ (ميلي: `ops/design-v2-ref/` مع سطر في المواصفة).
- [ ] توحيد صغير مرصود: `OperatorShell` يعرض شارة العدّ خاماً بينما `AdminShell` عبر `fmtCount` — توحيدهما (الناتج متطابق اليوم لكن التماثل نظافة).

## ب. تحسينات صغيرة عالية القيمة (مرصودة بتقارير المحققين)

- [ ] **`KpiTile` أسير `.m-skin`** (يعتمد متغيرات `--m-*`) — فكّ أسره ليُستعمل خارج معرض المكوّنات، أو توثيق القيد حيث هو.
- [ ] **دقة عدّ MCT في تنبيه أثر العطل** (دين مرحلة العطل: «N قضية كلف يعبر التاريخ نافذتها» بدل السطر العام).
- [ ] **`RESET` في api-mode** يستبدل الحالة المرطّبة ببذرة محلية — حجب زرّه هناك أو توثيقه للمستخدم.
- [ ] **سياسة العطل الرجعية**: العميل يمنع الماضي والخادم يقبله — توحيد الحكم (يميل المستشار للسماح بمسوّغ) — **قرار عميل**، يُعرض لا يُنفَّذ.
- [ ] حالات فراغ `EmptyState` المتبقية على `.ad-empty-inline` اليدوي في `EntityProfile`(2)/`UserProfile`(4)/`Operators`(1) — هجرة مؤجلة من دفعة قديمة.
- [ ] **الهيكل العظمي `.sk*` + `@keyframes shimmer`** (دَين v2؛ هذا الملف وجهته الوحيدة). `DESIGN-V2-SPEC` §٤-ي و§٧-ب-10 كانا يقولان «✅ يُبنى في BUILD-2»، وصُحِّحا إلى «مؤجَّل — لا مستهلك بعد»: لا شاشة **تُعلن** انتظارها اليوم — المتجر محلّي، وفي وضع الـAPI يبدأ من `emptyState()` ثم يُرطَّب بلا راية «قيد التحميل» (`store.tsx`)، و`EmptyState`/`PaginationBar` لا يأخذان حالة جلب. الشرط الحاكم: **يُبنى مع أوّل سطح يعلن الانتظار** (راية ترطيب في `StoreProvider` أو ما يعادلها)، لا قبله — أصناف بلا مستدعٍ تحظرها قاعدة «صفر غير مستعمل». المواصفة تحمل النصّ الأصلي (200ms، عدد الأسطر = حجم الصفحة، لا دوّارات)، و`tokens.css` يشير إلى هنا عند موضع الإطار المحذوف.
- [x] **سُدِّد 2026-08-30 (دفعة د٣ من DESIGN-FINISH-PLAN):** الطيّ مبني في الصدفتين (`masaar.nav.min` عبر `shellNav.tsx`، `aria-expanded`، أسماء وصولية وعدّ لا يُحذف، `--side-w-min` مصروف من التوكن) — الفحص المتصفحي المرافق ما زال ديناً مع جولة ٢×٢ المؤجلة. النص الأصلي: **الرفّ المطويّ 72px (`--side-w-min`)** (دَين v2؛ هذا الملف وجهته الوحيدة). التوكن معرَّف في `tokens.css` بلا مستهلك، وشقيقه `--side-w` مصروف — والاثنان زوج واحد لمكوّن واحد فيبقيان معاً. الطيّ **سلوك لا سطح**: يحتاج زرّ طيّ في تذييل الشريط (`aria-expanded`)، وتفضيلاً مخزَّناً `masaar.nav.min`، و`[data-side='min']` على `.op-shell` يبدّل عمود الشبكة — وكلّها خارج نطاق «مرئي فقط» الذي حكم موجة v2. الفحص المتصفّحي المرافق (`DESIGN-V2-SPEC` §٧-هـ-٣: الأيقونات مركّزة، التلميحات تعمل، حلقة البؤرة تُقرأ على التدرّج) **مؤجَّل معه** ويُنفَّذ يوم يُبنى.

## ج. بقايا خطة المنهجية الأصلية (مفتوحة من قبل موجة العميل)

- [ ] **شاشة إعدادات** (٦ب الأصلية): اللغة + حجم الخط الجذري «وضع كبار السن» + التباين العالي + **مفتاح الثيم v2 الجديد ينضم إليها** — تصبح ذات قيمة مضاعفة بعد الوضع الداكن.
- [ ] **القشرة القديمة المتبقية**: `PathsGuide` و`Audit` (الأصغران — 71/39 سطراً) ما زالا داخل `.op-legacy`؛ هجرتهما تُميت الغلاف نهائياً وتحذف `op-legacy` css.
- [ ] `SectionExplainer` على بقية أقسام الإدارة (وُلد في سلسلة الموافقات؛ الامتثال الزمني أخذه؛ تعميمه على العقود/الجهات/الوصول).
- [ ] **بند «الموافقات» في بوابة المشغّل** (دين v2-الشريط: لا مسار `#/operator/approvals` — إن أراد العميل رؤية مشغّله لسلّمه، تُبنى نسخة نطاقية).

## د. ديون الخادم المسماة (تُعرض على العميل — ليست تحسينات صامتة)

نقل حرفي من جداول الديون القائمة: كتابة `fields`/`service-contracts` خادمياً · `/operators` CRUD · تعديل سقفَي السلّم بفعل محوكم + مصدر خادمي واحد **(اتسع 2026-08-31 بموجة د٩: سلالم لكل مشغّل `operatorTiers` تعمل محلياً كاملة — `SET_OPERATOR_TIERS` ذاتي التدقيق + محرر Operators بمعاينة أثر؛ المطلوب خادمياً: نموذج تخزين override + endpoint محوكم + قراءة `assertTierAuthority` من السلّم المحلول لكل مشغّل بدل `DEFAULT_APPROVAL_TIERS` — عندها يُرفع حجب المحرر في api-mode)** · رفع مستندات حقيقي (multipart + نموذج) · «تذكير» بنقطة نهاية · تحرير كتالوجات §9 · تفضيلات مستخدم · هجرات Prisma الأساس (لا baseline).

## قاعدة التنفيذ

كل بند: تنفيذ ← تحقق ثلاثي ← بند commit مستقل على `design-v2` (أو فرع نظافة لاحق) — ولا يُنفَّذ بند «قرار عميل» بلا كلمته.
