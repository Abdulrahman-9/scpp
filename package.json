{
  "name": "masaar",
  "private": true,
  "version": "0.1.0",
  "description": "Masaar (مسار) — bilingual contractor & tender management platform for SCPP Rev 1.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit -p packages/working-days && tsc --noEmit -p packages/scpp-rules && tsc --noEmit -p packages/ui && tsc --noEmit -p packages/db && tsc --noEmit -p apps/web && tsc --noEmit -p apps/api",
    "build": "npm run build -w @masaar/web",
    "dev": "npm run dev -w @masaar/web",
    "dev:api": "npm run dev -w @masaar/api",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:generate": "npm run generate -w @masaar/db",
    "db:migrate": "npm run migrate -w @masaar/db",
    "db:seed": "npm run seed -w @masaar/db"
  },
  "devDependencies": {
    "@testing-library/react": "^16.1.0",
    "jsdom": "^25.0.1",
    "typescript": "^5.7.2",
    "vitest": "^3.0.0"
  }
}
